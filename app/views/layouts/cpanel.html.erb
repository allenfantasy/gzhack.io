<!DOCTYPE html>
<html>
<head>
  <title>Guangzhou Hackathon 2014</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body id="<%= controller_name %>" class="cpanel">

<div class="flash">
  <% flash.each do |type, message| %>
    <%= content_tag(:div, message, :class => "alert-#{type}") %>
  <% end %>
</div>

<% if user_signed_in? %>
<nav class="navbar navbar-inverse" role="navigation">
  <div class="collapse navbar-collapse" id="navbar-collapse-01">
    <ul class="nav navbar-nav">
      <li><%= link_to "后台主界面", cpanel_path %></li>
      <li><%= link_to "活动主页", root_path %></li>
      <li><%= link_to "报名列表", cpanel_users_path %></li>
      <li><%= link_to "活动列表", cpanel_events_path %></li>
    </ul>
  </div>
<!-- Navbar content -->
</nav>

<%= yield %>

<% else %>
  <%= form_tag cpanel_session_path, :method => :post do %>
    <div>
      <%= text_field_tag :username, nil, :placeholder => "Username" %>
    </div>
    <div>
      <%= password_field_tag :password, nil, :placeholder => "Password" %>
    </div>
    <div>
      <%= submit_tag "Sign In" %>
    </div>
  <% end %>
<% end %>

</body>
</html>
